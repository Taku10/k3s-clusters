apiVersion: v1
kind: Service
metadata:
  name: fairshare-client
  namespace: nonprod

spec:
  selector:
    app: fairshare-client

  ports:
    - name: http
      port: 80
      targetPort: 80

  type: ClusterIP