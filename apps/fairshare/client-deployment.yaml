apiVersion: apps/v1
kind: Deployment
metadata:
  name: fairshare-client
  namespace: nonprod

spec:
  replicas: 1

  selector:
    matchLabels:
      app: fairshare-client

  template:
    metadata:
      labels:
        app: fairshare-client

    spec:
      containers:
        - name: fairshare-client
          image: ghcr.io/taku10/fairshare-client:latest

          ports:
            - containerPort: 80