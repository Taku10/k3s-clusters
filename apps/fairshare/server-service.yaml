apiVersion: v1
kind: Service
metadata:
  name: fairshare-server
  namespace: nonprod

spec:
  selector:
    app: fairshare-server

  ports:
    - name: http
      port: 3000
      targetPort: 3000

  type: ClusterIP